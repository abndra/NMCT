# ===============================================================
#  NMCT — خدمة التحقق البنكي (Railway)
#  انسخ هذا الملف إلى .env محلياً، أو أضف نفس المتغيرات في Railway › Variables
#  لا تضع أي قيمة حقيقية في GitHub.
# ===============================================================

# منفذ التشغيل (Railway يضبطه تلقائياً)
PORT=3000

# كلمة سر طويلة تخترعها أنت. نفس القيمة تُكتب في لوحة التحكم › التحقق البنكي › توكن الخدمة
TOKEN=

# مفتاح تشفير (64 حرف hex). أنشئه بالأمر:  openssl rand -hex 32
# يُستخدم لتشفير refresh token الخاص بـ Gmail قبل حفظه في قاعدة البيانات
ENCRYPTION_KEY=

# ---------- Google OAuth (من Google Cloud Console › Credentials) ----------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# رابط الخدمة العام على Railway بدون / في النهاية، مثال: https://nmct-bank-verify.up.railway.app
PUBLIC_URL=
# (اختياري) إن تركته فارغاً يصبح: PUBLIC_URL + /oauth/google/callback
GOOGLE_REDIRECT_URI=

# رابط الموقع (يُستخدم للرجوع بعد ربط Gmail وللسماح بالاتصال CORS)، مثال: https://nmct.netlify.app
SITE_URL=
# (اختياري) أصول إضافية مسموح لها بالاتصال مفصولة بفواصل
ALLOWED_ORIGINS=

# ---------- Firebase Realtime Database ----------
FIREBASE_DB_URL=https://nmct-4d2a9-default-rtdb.firebaseio.com
# سر قاعدة البيانات: Firebase › Project settings › Service accounts › Database secrets
FIREBASE_DB_SECRET=
# مفتاح الويب (عام) — يُستخدم فقط للتحقق من هوية المستخدم المسجّل
FIREBASE_API_KEY=AIzaSyB0AOQwMAblWOcw-xYeMvTXwrdm3aoFlC4

# ---------- إعدادات التحقق ----------
# مدة التحقق بالدقائق (المطلوب: 3)
VERIFY_WINDOW_MINUTES=3
# كم دقيقة قبل إنشاء الطلب نقبل فيها وصول إشعار البنك (العميل عادةً يحوّل ثم ينشئ الطلب)
MATCH_LOOKBACK_MINUTES=15
# كل كم ثانية تُفحص الرسائل الجديدة
POLL_INTERVAL_SECONDS=10
# (اختياري) عنوان البريد الذي ترسل منه رسائل البنك لتضييق البحث، مثال: alerts@bankmuscat.com
GMAIL_SENDER=
# (اختياري) استعلام Gmail مخصص بدل الافتراضي
GMAIL_QUERY=

# ---------- اختياري: إرسال رسالة واتساب للعميل بعد نجاح الشحن ----------
STORE_NAME=NMCT
WA_SERVER_URL=
WA_TOKEN=
